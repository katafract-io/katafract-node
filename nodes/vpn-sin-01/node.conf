# Node-specific configuration for vpn-sin-01
# Applied by install.sh after common/ — values here override common defaults.
# No secrets — private keys and peer lists are managed by Artemis at runtime.

NODE_ID="vpn-sin-01"
WG_SERVER_IP="10.10.3.1"
WG_SUBNET="10.10.3.0/24"
REGION="apac-sin"
SITE="Singapore"
AGH_PORT="3000"
UNATTENDED_REBOOT_TIME="20:00"
