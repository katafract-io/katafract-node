# Node-specific configuration for vpn-us-east-01
# Applied by install.sh after common/ — values here override common defaults.
# No secrets — private keys and peer lists are managed by Artemis at runtime.

NODE_ID="vpn-us-east-01"
WG_SERVER_IP="10.10.6.1"
WG_SUBNET="10.10.6.0/24"
REGION="us-east"
SITE="Ashburn, VA"
AGH_PORT="3000"
UNATTENDED_REBOOT_TIME="03:00"
