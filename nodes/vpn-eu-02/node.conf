# Node-specific configuration for vpn-eu-02
# Applied by install.sh after common/ — values here override common defaults.
# No secrets — private keys and peer lists are managed by Artemis at runtime.

NODE_ID="vpn-eu-02"
WG_SERVER_IP="10.10.2.1"
WG_SUBNET="10.10.2.0/24"
REGION="eu-north"
SITE="Helsinki, FI"
AGH_PORT="3000"
UNATTENDED_REBOOT_TIME="03:30"
